name: GitFolder
options:
  bundleIdPrefix: app.hakobs
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "16.0"
  generateEmptyDirectories: true

settings:
  base:
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: 2
    SWIFT_VERSION: "5.0"
    DEVELOPMENT_TEAM: "38MGF83L2L"

packages:
  # iOS transport: hosted git provider REST API via git-pont (no local clone / no shell).
  git-pont:
    url: https://github.com/silvandiepen/git-pont.git
    branch: codex/initial-git-pont-monorepo
  # Shared Swift helpers (KeychainService).
  GitKittieKit:
    path: ../../swift/GitKittieKit

targets:
  GitFolder:
    type: application
    platform: iOS
    sources:
      - path: GitFolder
    dependencies:
      - package: git-pont
        product: GitPontCore
      - package: git-pont
        product: GitPontGitHub
      - package: git-pont
        product: GitPontGitLab
      - package: GitKittieKit
        product: GitKittieKit
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: app.hakobs.gitfolder
        INFOPLIST_FILE: GitFolder/Info.plist
        TARGETED_DEVICE_FAMILY: "1,2"
        CODE_SIGN_STYLE: Automatic
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    info:
      path: GitFolder/Info.plist
      properties:
        CFBundleDisplayName: GitFolder
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        LSRequiresIPhoneOS: true
        ITSAppUsesNonExemptEncryption: false
        # Allows detecting whether ImageKid is installed (for "Edit in ImageKid").
        LSApplicationQueriesSchemes:
          - imagekid
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        UISupportedInterfaceOrientations~ipad:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
